#1️⃣ What is the difference between var, let, and const?

var is not used nowadays. declaring anything using var can be accessed from anywhere of the code.
let confines the variables inside its local scope
using const to declare will not allow to further assigning any value

#2️⃣ What is the spread operator (...)?

its an useful feature which helps to access all the elements of an array or object individually. basically it returns elemnt not as an array but individual

#3️⃣ What is the difference between map(), filter(), and forEach()?

map() is used if we want to apply any operation in all the elements of an array or object etc. 
filter() is used to get all the elements of that array or object that matches the condition
forEach() is used to loop through all the elements

#4️⃣ What is an arrow function?

its an feature of es6. Easier way to write anonymous functions. 
const arrowFunc = ()=>{
    ##we can do all the things that we can do with named functions
}

#5️⃣ What are template literals?

its an easier way to write multi line html and use of variables in a dynamic way